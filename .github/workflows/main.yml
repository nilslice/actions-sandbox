name: sandbox

on:
  release:
    types: [created]

jobs:
  build:
    strategy: 
      matrix:
        os: [ubuntu-latest, macos-latest]
        
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          TAG=${{ github.event.release.tag_name }}
          OS=$(uname -s | awk '{print tolower($0)}')
          ARCH=$(uname -m)
          SHA=${{ github.sha }}
          echo release-${OS}-${ARCH}-${TAG}-${SHA} >> Release.txt
          
      - name: Test
        run: cat Release.txt
        
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: Release.txt
